name: build_and_publish

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v1
        with:
          node-version: '16.x'

      - name: '1'
        run: 'ls -la'

      - name: install
        run: |
          npm install

      - name: build
        run: |
          npm run build
      - name: '1'
        run: 'ls -la'
